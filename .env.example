# Leave this set to True if running outside of production.
ENVIRONMENT = local

WEBSITE_DOMAIN = "artcc.com"
ARTCC_ICAO = "ICAO"

# Generate a random Django secret key and enter it here
SECRET_KEY = "abc123"

DB_HOST = "127.0.0.1"
DB_PORT = 5432
DB_NAME = "name"
DB_USER = "username"
DB_PASS = "password"

EMAIL_HOST = "example.com"
EMAIL_PORT = 465
EMAIL_USER = "username"
EMAIL_PASS = "password"

EMAIL_USE_TLS = False

# VATUSA authentication
API_KEY = "abc123"
ULS_K_VALUE = "abc123"

# Insert the ID of the ULS redirect URL you wish to use for the site.
ULS_REDIR_URL = 6

# Add all controlled (i.e., towered) fields in your ARTCC here.
# If your CTR/FSS positions use a prefix that does not align with one of your airport codes
# e.g. CHI_CTR, add that prefix to this list as well.
CONTROLLED_FIELDS = "['IATA', 'IATA', 'IATA', 'IATA']"

# Controller activity requirements (hours)
HOME_ACTIVITY_REQUIREMENT = 2
VISITOR_ACTIVITY_REQUIREMENT = 1
